# Don't use a pager for long AWS results
export AWS_PAGER=

# Allow AWS SDKs to load region from ~/.aws/config
export AWS_SDK_LOAD_CONFIG=1

# Disable telementry in AWS SAM
export SAM_CLI_TELEMENTRY=0
